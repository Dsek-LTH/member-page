query MyChest($memberId: UUID!) {
  chest(memberId: $memberId) {
    id
    items {
      id
      name
      description
      paidPrice
      imageUrl
      variant
      category {
        id
        name
        description
      }
      paidAt
      consumedAt
    }
  }
}
