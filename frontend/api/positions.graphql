query GetPositions($committeeId: UUID) {
  positions(filter: { committee_id: $committeeId }, perPage: 1000) {
    positions {
      id
      name
      nameEn
      committee {
        name
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
