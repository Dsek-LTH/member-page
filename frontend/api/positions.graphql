query GetPositions($committeeId: UUID) {
  positions(filter: { committee_id: $committeeId }) {
    positions {
      id
      name
      committee {
        name
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
