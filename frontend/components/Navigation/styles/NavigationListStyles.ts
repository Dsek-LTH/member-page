import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  menuBar: {
    minHeight: '48px!important',
    height: '48px',
  },
  menuDetails: {
    paddingTop: 0,
    paddingBottom: 0,
  },
}));
