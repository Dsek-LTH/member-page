import { Theme } from '@mui/material';
import { makeStyles } from '@mui/styles';

export const memberEditorStyles = makeStyles((theme: Theme) => ({
    updateButton: {
        marginTop: theme.spacing(2)
    },
    stack: {
        marginTop: theme.spacing(2)
    }
}));
