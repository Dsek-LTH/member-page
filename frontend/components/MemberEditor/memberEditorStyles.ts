import { makeStyles } from '@mui/styles';

export const memberEditorStyles = makeStyles(theme => ({
    updateButton: {
        marginTop: theme.spacing(2)
    },
    stack: {
        marginTop: theme.spacing(2)
    }
}))