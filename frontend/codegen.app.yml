overwrite: true
# Change this to our sandbox API when introspection is enabled
schema: "https://graphql.api.sandbox.dsek.se/"
documents: "./app_api/*.graphql"
generates:
  app_api/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
