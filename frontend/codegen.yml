overwrite: true
schema:  ${NEXT_PUBLIC_GRAPHQL_ADDRESS}
documents: "./**/*.graphql"
generates:
  generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
