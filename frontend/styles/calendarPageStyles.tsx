import { makeStyles } from '@material-ui/styles';

export const calendarPageStyles = makeStyles(theme => ({
    day: {
        borderStyle: 'solid',
        borderColor: '#e5e7eb'
    },
    event: {
        
    } 
}))