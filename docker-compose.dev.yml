version: "3"
volumes:
  data1-1:
services:
  core:
    build:
      dockerfile: ./backend/services/core/Dockerfile
    volumes:
      - ./backend/services/core:/project/app
  frontend:
    build:
      dockerfile: ./frontend/Dockerfile
    volumes:
      - ./frontend:/project/app
  minio1:
    volumes:
      - data1-1:/data1
